{
  "name": "Migration explorer",
  "install": "npm ci && npm run build",
  "terminals": [
    {
      "name": "wrangler dev",
      "command": "npx wrangler dev --ip 127.0.0.1 --port 8787",
      "description": "Cloudflare Worker + static assets dev server (http://127.0.0.1:8787)"
    }
  ],
  "ports": [
    { "name": "wrangler dev", "port": 8787 }
  ]
}
